

Auswahl Verzeichnis
Berechnung EVAL

	Datum: fixes Datum oder Installation DB <== day0
	kleinerer Wert gewinnt
	Konfig: Interpretation 0 Sterne als x (2) Sterne!

	Items:
		eval_time = mt * (last_played - day0)
			kleinerer Wert gewinnt

		eval_rating = -(m)r * rating
			negativ: kleinerer Wert gewinnt

		gesamt = mt * (last_played - day0) - mr * rating

		mt == 1 /(fix)

		mr == 60 (x1 Stern gleich 60 Tage aus)

		Markierung "skip" !

	Verzeichnis
		Anforderung
			items werden sortiert gespeichert!

		eval_time = mt * (last_played - day0)
			kleinerer Wert gewinnt

		avg-item = Durchschnitt der 10 kleinsten Item-Werte

		gesamt = mt * (last_played - day0) - ma * avg-item - mc * count

			mt == 1 (fix?)
			ma == 60 (wie Items?)
			mc == 1 / batch_count

		- Wert für Items

-------------------------------------------------------------------------

Auswahl Items
	Anforderung
		items werden sortiert gespeichert!

	Wahrscheinlichkeit aus math-Funktionier

	Beispiel:
		20 Item
		selection = 20 - Wurzel(Zufalls-400);


	keine zusammenhängenden Items?

		eval


-------------------------------------------------------------------------

Prio 1
	öffnen

	check for cached results => deliver
	+ find new ones

Prio 2
	meta

Prio 3
	fill autoCache

Prio 3
	Prüfe Konfiguration: Wenn Änderungen gespeicherte Stand wegwerfen und komplett neu aufsetzen
		Task wird vom System beim Start neu eingestellt
		wenn Konfiguration geändert oder letzter Durchlauf x Tage her ist
		Nezustart-Task Einstellen, wenn schon lange her ist
			Task-Liste leeren: Prio 4 - LAST
			Neustart-Task einstellen

Prio 4
	Neu-Berechnung Verzeichnis anhand gespeicherter Daten
		Argument: aktuell angezeigte Datei

Prio 5
	Meta-Daten einlesen und abspeichern
		n-Items
		Eval für Verezichnis neu berechnen

Prio 6
	Verzeichnis aktualisieren
		Agrument: neu Verzeichnis
		diese Verzeichnis-Task-Liste wird gespeichert

		löschen, wenn nicht mehr existiert
		Objekt aus DB laden + wenn nicht in DB anlegen

		Dateien abgleichen
			wenn Datumstempel geändert => meta neu einlesen

		neue Tasks erzeugen - Meta-Einlesen - n-Items

Prio 7
	Neustart-Task
		Task-Liste (Dirs) neu füllen => Prio 6










-------------------------------------------------------------------------

DB

	saveStatus
	loadStatus

	saveDir
	loadDir

	getDirsSorted


-------------------------------------------------------------------------
